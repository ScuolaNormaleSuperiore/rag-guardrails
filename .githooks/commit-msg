#!/usr/bin/env bash
set -euo pipefail

echo "[RAG-GUARDS commit-msg] Checking commit message..." >&2

if grep -Eiq '^Co-Authored-By:[[:space:]]*(Claude|ChatGPT|Codex|OpenAI)([[:space:]]|$)' "$1"; then
	echo "Commit blocked: remove AI Co-Authored-By trailer(s)." >&2
	exit 1
fi
